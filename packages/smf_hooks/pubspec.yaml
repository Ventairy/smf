name: smf_hooks
description: Lightweight typed hook SDK for SMF release automation.
version: 1.1.0
repository: https://github.com/Ventairy/smf
issue_tracker: https://github.com/Ventairy/smf/issues
topics:
  - release
  - automation
  - hooks

environment:
  sdk: ">=3.10.0 <4.0.0"

platforms:
  linux:
  macos:
  windows:

resolution: workspace

dev_dependencies:
  path: ^1.9.1
  # Keep aligned with smf_engine; see its analyzer compatibility note.
  test: 1.31.2
