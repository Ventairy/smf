name: smf_engine
description: Release planning and Apple and Android delivery for SMF.
version: 1.1.0
repository: https://github.com/Ventairy/smf
issue_tracker: https://github.com/Ventairy/smf/issues
topics:
  - flutter
  - ios
  - android
  - release
  - automation

environment:
  sdk: ">=3.10.0 <4.0.0"

platforms:
  linux:
  macos:
  windows:

resolution: workspace

dependencies:
  crypto: ^3.0.7
  dart_jsonwebtoken: ^3.4.1
  freezed_annotation: ^3.1.0
  googleapis: ^16.0.0
  googleapis_auth: ^2.3.3
  http: ^1.5.0
  json_annotation: ^4.12.0
  path: ^1.9.1
  pub_semver: ^2.2.0
  # Workspace resolution uses the local package; pub.dev consumers use this
  # hosted constraint.
  smf_hooks: ^1.0.0
  xml: ^7.0.1
  yaml: ^3.1.3

dev_dependencies:
  build_runner: 2.16.0
  freezed: ^4.0.0-dev.3
  frontend_server_client: ^4.0.0
  json_serializable: ^6.14.0
  # test 1.31.x requires analyzer 13+, while the latest freezed requires
  # analyzer <11.
  test: 1.31.2
